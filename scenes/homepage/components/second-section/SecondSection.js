import FirstAddSection from "../../../../shareable/FirstAddSection";

const SecondSection = ({ sectionTextData }) => {
  return (
    <>
      <FirstAddSection sectionTextData={sectionTextData} />
    </>
  );
};

export default SecondSection;
