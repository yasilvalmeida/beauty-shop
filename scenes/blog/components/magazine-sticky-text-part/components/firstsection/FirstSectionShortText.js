const FirstSectionShortText = ({ content_italic_two, content_three }) => {
  return (
    <div className={"first__section__short__text"}>
      <i>{content_italic_two}</i>
      <p>{content_three}</p>
    </div>
  );
};

export default FirstSectionShortText;
